name: Publish Package
on:
  push:
    tags:
      - "v[0-9]+.[0-9]+.[0-9]+"
      - "v[0-9]+.[0-9]+.[0-9]+-dev.[0-9]+"

jobs:
  build-android:
    name: "Build Demo (Android)"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./example
    permissions:
      id-token: write
      contents: read
      attestations: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Java 21
        uses: actions/setup-java@v6
        with:
          distribution: "temurin"
          java-version: "21"
          cache: "gradle"
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          cache: true
      - name: Build Android app
        run: flutter build apk --dart-define=COMMIT_SHA=${{ github.sha }}
      - name: Rename output APK
        run: mv build/app/outputs/apk/release/app-release.apk build/app/outputs/apk/release/android-demo.apk
      - name: Upload as artifact
        uses: actions/upload-artifact@v7
        with:
          path: example/build/app/outputs/apk/release/android-demo.apk
          if-no-files-found: error
          archive: false
      - name: Generate artifact attestation
        uses: actions/attest@v4
        with:
          subject-path: example/build/app/outputs/apk/release/android-demo.apk

  build-windows:
    name: "Build Demo (Windows)"
    runs-on: windows-latest
    defaults:
      run:
        working-directory: ./example
    permissions:
      id-token: write
      contents: read
      attestations: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          cache: true
      - name: Build Windows app
        run: flutter build windows --dart-define=COMMIT_SHA=${{ github.sha }}
      - name: Generate app installer
        run: iscc "windows/installer-config.iss"
      - name: Upload as artifact
        uses: actions/upload-artifact@v7
        with:
          path: example/build/windows/output/windows-demo.exe
          if-no-files-found: error
          archive: false
      - name: Generate artifact attestation
        uses: actions/attest@v4
        with:
          subject-path: example/build/windows/output/windows-demo.exe

  build-web:
    name: "Build & Deploy Demo (Web)"
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./example
    permissions:
      id-token: write
      contents: read
      attestations: write
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          cache: true
      - name: Build web app
        run: flutter build web --wasm --dart-define=COMMIT_SHA=${{ github.sha }}
      - name: Compress web build
        run: zip -r web-demo.zip build/web
      - name: Upload as artifact
        uses: actions/upload-artifact@v7
        with:
          path: example/web-demo.zip
          if-no-files-found: error
          archive: false
      - name: Generate artifact attestation
        uses: actions/attest@v4
        with:
          subject-path: example/web-demo.zip

  gh-release:
    name: "Create GitHub Release"
    runs-on: ubuntu-latest
    needs: [build-android, build-windows, build-web]
    permissions:
      contents: write
    steps:
      - name: Fetch build artifacts
        uses: actions/download-artifact@v8
        with:
          path: artifacts/
          merge-multiple: true
      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          files: artifacts/*
          prerelease: ${{contains(github.ref_name, '-dev')}}
          generate_release_notes: true

  pub-publish:
    name: "Publish To pub.dev"
    needs: [build-android, build-windows, build-web]
    permissions:
      id-token: write
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@v1
    with:
      environment: "pub.dev"
